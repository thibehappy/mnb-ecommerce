node_modules
.next
out
build
dist

.env
.env.local
.env.*.local

.DS_Store
Thumbs.db

*.log
.vscode
.idea

coverage
test-results
playwright-report
playwright/.cache

# TypeScript incremental builds
*.tsbuildinfo

# Raw / source-only photos waiting to be detoured. Contributors drop
# huge JPGs/PNGs here ; the pipeline produces lighter detoured copies
# in beads/, charms/, figurines/, attachments/. Don't commit the
# multi-megabyte sources — they'd bloat the repo for no runtime gain.
public/photos/other/
